type Theme = {
  bg: string;
  textColor: string;
  ringColor: string;
  cardColor: string;
};

export const lightTheme: Theme = {
  bg: "white",
  textColor: "#1E2329",
  ringColor: "#FCD535",
  cardColor: "#FAFAFA",
};

export const darkTheme: Theme = {
  bg: "#181A20",
  textColor: "#EAECEF",
  ringColor: "#FCD535",
  cardColor: "#1E2329",
};
